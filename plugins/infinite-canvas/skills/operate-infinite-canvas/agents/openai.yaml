interface:
  display_name: "Operate Infinite Canvas"
  short_description: "Open Infinite Canvas, upload media, and control workflows"
  default_prompt: "Use $operate-infinite-canvas to open Infinite Canvas in Chrome, upload my local media, then inspect and update my active canvas."
dependencies:
  tools:
    - type: "mcp"
      value: "infinite-canvas"
      description: "Live Infinite Canvas browser control tools"
policy:
  allow_implicit_invocation: true
