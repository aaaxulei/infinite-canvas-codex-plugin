interface:
  display_name: "制作图像模板"
  short_description: "推断适配模特、对照参考图并构建可验收模板工作流"
  default_prompt: "使用 $create-image-templates 推断适配参考图气质的素人模特，生成付费样片并对照参考图，必要时自动制作 V2，验收后再生成免费版。"
dependencies:
  tools:
    - type: "mcp"
      value: "infinite-canvas"
      description: "Live Infinite Canvas browser control tools"
policy:
  allow_implicit_invocation: true
